server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/enrichplus
    username: enrich_user
    password: enrich_pass
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

clients:
  demand-service-url: http://localhost:8081
  staffing-service-url: http://localhost:8082
