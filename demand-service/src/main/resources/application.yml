server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/enrichplus
    username: enrich_user
    password: enrich_pass
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
